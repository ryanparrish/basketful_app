services:
  participant-frontend:
    image: ${DOCKER_USERNAME}/basketful-participant:${FRONTEND_IMAGE_TAG:-latest}
    pull_policy: always
    ports:
      - "${PARTICIPANT_FRONTEND_PORT:-8082}:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
